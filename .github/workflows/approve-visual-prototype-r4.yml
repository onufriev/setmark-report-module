name: Approve VISUAL_PROTOTYPE R4

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  approve-r4:
    runs-on: ubuntu-latest
    steps:
      - name: Approval already recorded
        run: echo "VISUAL_PROTOTYPE-R4 approval is already recorded in product/phase-reviews.json"
